{
  "name": "mathblaster",
  "version": "1.0.0",
  "description": "MathBlaster — Contra-style math puzzle shooter for Android",
  "scripts": {
    "icons":     "node generate-icons.js",
    "dev":       "npx serve game -l 3000 --cors",
    "cap:init":  "npx cap init MathBlaster com.mathblaster.game --web-dir game",
    "cap:add":   "npx cap add android",
    "cap:sync":  "npx cap sync",
    "cap:open":  "npx cap open android",
    "android":   "npm run cap:sync && npm run cap:open"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "canvas": "^2.11.2",
    "serve": "^14.2.0"
  }
}
